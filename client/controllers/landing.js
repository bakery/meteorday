LandingController = RouteController.extend({
    template: 'landing',

    waitOn: function () {
    },

    data: function () {
        return {};
    }

    // action: function () {
    //  if we want to override default behavior 
    // }
});